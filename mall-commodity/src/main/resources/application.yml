spring:
  datasource:
    username: root
    password: guoxin
    url: jdbc:mysql://192.168.6.128:3306/mall_pms
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 10000